To try it:

sh
cd ~/nahimic-linux && git pull && cd packaging && makepkg -si
systemctl --user reset-failed nahimic.service
pkill -f 'Nahimic playback'; systemctl --user restart nahimic.service
sleep 5; nahimic --status

Two notes:

Your ~/.config/nahimic-linux/devices.json is being used (the log says "Other laptop", the example name from the README). It works, but the built-in table now covers your hardware, so you can delete that file if you want.
This may not be the whole story. The second crash was the host exiting with code 1, which might be a separate problem. If it fails again, send failure-graph.txt plus host.log and capture.log from ~/.local/share/nahimic-linux/runtime/.